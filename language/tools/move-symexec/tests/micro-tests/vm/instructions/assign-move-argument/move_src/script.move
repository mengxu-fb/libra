script {
    fun main(a: bool, b: u8, c: u64, d: u128, e: address, f: vector<u8>) {
        let _ = move a;
        let _ = move b;
        let _ = move c;
        let _ = move d;
        let _ = move e;
        let _ = move f;
    }
}
