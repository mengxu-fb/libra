script {
    fun main(a: bool, b: u8, c: u64, d: u128, e: address, f: vector<u8>) {
        let _ = copy a;
        let _ = copy b;
        let _ = copy c;
        let _ = copy d;
        let _ = copy e;
        let _ = copy f;
    }
}
